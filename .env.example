# agent-context-doctor has no required environment variables.
# It makes no external network calls and requires no API keys.
#
# This file exists only to document that fact and to satisfy
# tooling that expects a .env.example in open-source repos.
